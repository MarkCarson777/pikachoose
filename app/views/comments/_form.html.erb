<%= simple_form_for([@message, @message.comments.build]) do |f| %>
  <%= f.input :content, label: "Type your reply:" %>
  <%= f.button :submit, class: "btn btn-primary mt-2 mb-2 mr-sm-2 text-light"  %>
<% end %>
